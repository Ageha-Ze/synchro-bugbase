# TODO: Fix NewBugModal.tsx Insert Error

## Steps to Complete
- [x] Add `user_id: user.id` to the `bugData` object in `NewBugModal.tsx` to resolve the "record 'new' has no field 'user_id'" error.
- [x] Test creating a new bug to verify the fix works and no errors occur.
